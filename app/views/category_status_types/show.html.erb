<% content_for :title, 'Show Category Status Type' %>
<p id="notice"><%= notice %></p>

<p>
  <strong>Status text:</strong>
  <%= @category_status_type.status_text %>
</p>

<p>
  <strong>Order in list:</strong>
  <%= @category_status_type.order_in_list %>
</p>

<%= link_to 'Edit', edit_category_status_type_path(@category_status_type) %> |
<%= link_to 'Back', category_status_types_path %>
