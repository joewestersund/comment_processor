<%= form_with(model: comment_status_type, local: true) do |form| %>
  <% if comment_status_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_status_type.errors.count, "error") %> prohibited this comment_status_type from being saved:</h2>

      <ul>
      <% comment_status_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status_text %>
    <%= form.text_field :status_text, id: :comment_status_type_status_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
