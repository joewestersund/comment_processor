Dear <%= @assigned_to.name %>,

A comment response category called '<%= @category.category_name %>' has just been assigned to you in the comment processing database.

link: <%= @category_url %>

Category Name: <%= @category.category_name %>
Description: <%= @category.description %>
Response Type: <%= @category.category_response_type.response_text if @category.category_response_type.present? %>
Response Text: <%= @category.response_text %>
Action Needed: <%= @category.action_needed %>
Category Status: <%= @category.category_status_type.status_text if @category.category_status_type.present? %>
Assigned by: <%= "#{@assigned_by.name} <#{@assigned_by.email}>" %>